# Используем официальный образ Node.js (версию можно поменять, например, 18)
FROM node:20-alpine

# Устанавливаем рабочую директорию в контейнере
WORKDIR /app

# Копируем package.json и package-lock.json (если есть) для установки зависимостей
COPY package.json ./
COPY package-lock.json* ./

# Устанавливаем зависимости
RUN npm install

# Копируем папку src и остальные файлы проекта
COPY src ./src

# Если есть другие файлы (например, конфиги), копируем их
COPY . .

# Выполняем сборку (если требуется, например, для React или других фронтенд-проектов)
# Убери эту строку, если сборка не нужна
RUN npm run build

# Указываем порт, который будет использоваться (пример: 3000)
EXPOSE 3000

# Команда для запуска приложения
# Замени "start" на тот скрипт, который указан в твоем package.json
CMD ["npm", "run", "start"]