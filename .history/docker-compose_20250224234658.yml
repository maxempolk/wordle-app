version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    volumes:
      - ./backend/app:/app


  frontend:
    build: ./frontend
    ports:
      - "8080:8000" # Заменил 80 на 8080 для хоста


networks:
  app-network:
    driver: bridge